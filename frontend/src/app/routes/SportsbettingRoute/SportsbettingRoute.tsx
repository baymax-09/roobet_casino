import React from 'react'

import { GameRoute } from '../GameRoute'

export const SportsbettingRoute: React.FC = () => {
  return <GameRoute identifier="slotegrator:sportsbook-1" canFavorite={false} />
}
