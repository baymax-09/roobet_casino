import createStyles from '@mui/styles/createStyles'
import makeStyles from '@mui/styles/makeStyles'

export const useViewOnBlockchairStyles = makeStyles(theme =>
  createStyles({
    externalIcon: {
      marginLeft: theme.spacing(1),
    },
  }),
)
