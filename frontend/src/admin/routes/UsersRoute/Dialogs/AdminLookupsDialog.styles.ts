import makeStyles from '@mui/styles/makeStyles'
import { blue } from '@mui/material/colors'

export const useAdminLookupsDialogStyles = makeStyles({
  avatar: {
    backgroundColor: blue[100],
    color: blue[600],
  },
})
