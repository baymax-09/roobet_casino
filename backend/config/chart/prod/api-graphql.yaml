global:
  apm:
    enabled: true
  ingress:
    enabled: true
  metrics:
    enabled: true
  resources:
    limits:
      memory: 2Gi
    requests:
      cpu: 1000m
      memory: 2Gi

app:
  name: api-graphql
  type: deployment
  
  ip2location: true
  replicas: 20
  service: true
  paths:
    - /admin/graphql
    - /graphql
