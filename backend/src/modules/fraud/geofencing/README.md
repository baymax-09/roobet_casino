# Geofencing

This module provides us the ability to detect VPN IPs and use IP-based geolocation for geofencing features on the site.

## Testing

Make sure you have downloaded the IP2Proxy and IP2Location databases using the script `updateIpDbs.sh`.
